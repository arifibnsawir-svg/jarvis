# Jarvis Social Autopilot — Progress Checkpoint
Update: 10 Juli 2026 (WIB)
Status: Recon/Monitoring LIVE · Content+Council belum · Posting belum

## Tujuan
Jarvis jadi full asisten medsos (riset -> konten -> posting -> engagement), human-in-loop (ACC),
buat growth akun + jual buku "THE AI ARCHITECT".
Prinsip: evidence-first, anti-halu, anti-hype, sacred-IP fail-closed, on-niche "AI Architect".

## Arsitektur (LOCKED 9 Jul)
- Orkestrasi NATIVE Hermes (cron + webhooks + kanban dispatch + skills + deliver Telegram).
- n8n TURUN jadi opsional (LIVE via Docker 2.29.7, port 5678) - park buat glue visual/integrasi eksternal.
- Gateway Hermes localhost-only, di-expose tailnet-only via Tailscale Serve (bukan publik).
- Endpoint native yang dipakai: POST /api/webhooks (trigger), POST /api/cron/jobs (scheduler).
- 2 repo utama: arifibnsawir-svg/jarvis (core agent, ~/jarvis) + arifbudiman575-ship-it/buku (naskah+strategi).

## Infra Jalur B (verified 7 Jul)
- Scrapling 0.4.10 + fetchers (Reader), agent-browser 0.31.1 + Chrome 150 (Actor).
- Semua di venv/prefix user (~/.hermes/recon-venv + ~/.hermes/node).
- Jalur B (browser automation) = PRIMARY (baca kompetitor + reply, tanpa App Review). Wajib pelan/anti-barbar.

## Recon Engine (Stage 1 - DONE, production-ready)
- Skill modular-competitive-monitoring di-audit penuh (config-driven Scrapling YAML).
- Threads extraction DUAL-SOURCE: primary JSON Relay follower_count (integer eksak) + cross-check og:description.
- Login-wall root cause = BUG URL double-@ (fix lstrip('@')), BUKAN IP-block/UA.
- Cross-check K/M/B HARDENED (unit-tolerance) -> 6 akun MATCH.
- Anti-halu guard PROVEN: data gak yakin -> kosongin, jangan tulis angka.
- Own-account @arifb.id validated: 2 followers (real, dual-source).

## Kompetitor (Threads, live-confirmed 10 Jul)
- @hanifmuh_ ~166K, @lifeastechbro ~129K, @tommyteja ~119K, @productivityboi ~94K, @dimasyoga.pw ~66K
- Watchlist SOP-8 (repo buku): productivityboi, tommyteja, argitendo, rubyabdullah.ai, dimasyoga.pw,
  Raymond Chin, Fellexandro Ruby, RevoU.
- Constraint SOP: JANGAN auto-reply; data cuma buat re-hooking konten.

## Monitoring LIVE
- Cron competitor-recon-daily (30 6 * * *, 06:30 WIB) -> append history CSV, deliver local silent.
- Cron daily-report (0 7 * * *, 07:00 WIB) -> deliver Telegram, anti-halu.

## Beberes (10 Jul)
- Rogue cron arifb_daily_performance_tracker (lapor mock sbg real) DIHAPUS (config di-dump ke quarantine, recoverable).
- Mock CSV arifb_master_performance.csv di-quarantine.

## Temuan diagnosa (belum di-fix)
- Artefak backup: backup lokal jadi (2.6GB) tapi upload gdrive MATI sejak ~Jun.
  3 sebab: token OAuth expired + gate script 250MB skip + cron timeout 120s.
  Risiko: ~96GB lokal, nol off-site. Fix: reconnect token -> timed test -> set gate/timeout -> slim backup. (Di-hold.)
- competitor_daily_monitor (5 IG generik off-niche) paused; opsi repurpose/hapus.

## Repo & checkpoint
- Progress di-track di repo jarvis (docs/). Git auth Acer kepasang (gh + PAT).
- Master detail lengkap = Notion Master Plan.

## Next
1. [>=2 hari data] wire delta-follower ke daily report.
2. Bangun Stage 2 Content Pipeline + Gate/Council (jarvis-reason + sacred-IP rules) = prasyarat posting.
3. Fix backup gdrive (berurut, ukur dulu).
4. Konfirm watchlist final + riset handle kurang.
5. Fix Windows-path parse-daily-entry.py -> Linux.

Prinsip tetap: human-in-loop (ACC) di fase 0-follower; full-auto ditolak dulu.
